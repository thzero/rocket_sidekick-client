Suggestions

    Better validation on foam (and other tools) based on english/metric measurement unit selected.

For calculations instead of mathjs?
    https://theoephraim.github.io/node-google-spreadsheet/#/

Tools
    Parachute Sizing
        Lookup to the atompsheric chart based on altitude entry
        Allow selection of parachute and/or rocket once enabled
    Thrust2Weight
        Allow selection of rocket/setup once enabled

    https://www.translatorscafe.com/unit-converter/en-US/calculator/rocket-max-altitude/

    
    Flight Path/Info
        when file is loaded, try and detect and change the flight processor...

    Flight Path
        when wrong processor, or invalid file, need to show on UI better....
        Track previous, see about detecting spikes
            https://www.npmjs.com/package/kalman-filter?

Checklists
	potentially create a launch from a checklist...
        if creating a new launch from checklist, should navigate to it

    templates
        all defaults are templates
        boolean to turned a copied template/default into a template

Rocket Gallery
    show launches on rockets if marked

Rocket Setups
    finish

Lists
    // TODO: SECURITY: Admin can...
    isOwner - allow for any admin to delete and edit if 'public'
        need to also check on the back end if 'public'
    hasAdmin - needs a check...
        need to ask check on the back end if someone can set the public flag...
        started....
    edit open
        leave page
            check if dirty, and trap navigation

Deleting
    When getting a false due to item used in another object, make sure to display issue...

Export to excel

    does this give a better result?
    https://www.npmjs.com/package/vue-json-excel3

Searching
    text not working on motor?

Inventory
    Delay does not let 'P' get set
        probably a custom control since can't use alpha with the input type attribute of 'number'
    Grid output of the inventory in the browser?

Markup
    https://github.com/github/markup
    https://gist.github.com/ChrisTollefson/a3af6d902a74a0afd1c2d79aadc9bb3f
